#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory
set -x

wmake libso twoPhaseThermo
wmake
wmake compressibleInterDyMFoam

# ----------------------------------------------------------------- end-of-file
