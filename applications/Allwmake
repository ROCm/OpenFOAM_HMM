#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory
set -x

( cd solvers   && wmake all )
( cd utilities && wmake all )

# ----------------------------------------------------------------- end-of-file
