#!/bin/sh
cd ${0%/*} || exit 1    # Run from this directory

( cd throttle && ./Allrun $* )
( cd throttle3D && ./Allrun $* )

#------------------------------------------------------------------------------
