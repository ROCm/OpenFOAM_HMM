#!/bin/sh
set -x

wmake libso dummy

case "$WM_MPLIB" in
LAM | OPENMPI | MPI | MPICH | MPICH-GM | MPIGAMMA )
   export WM_OPTIONS=${WM_OPTIONS}$WM_MPLIB
   wmake libso mpi
   ;;

GAMMA)
   wmake libso gamma
   ;;
esac

