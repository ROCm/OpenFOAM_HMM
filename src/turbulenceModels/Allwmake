#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory
set -x

(cd LES && ./Allwmake )
(cd incompressible && ./Allwmake )
(cd compressible && ./Allwmake )

# ----------------------------------------------------------------- end-of-file
